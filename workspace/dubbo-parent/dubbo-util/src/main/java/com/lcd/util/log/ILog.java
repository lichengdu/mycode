package com.lcd.util.log;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public interface ILog
{
	public static Log log = LogFactory.getLog(ILog.class);
}